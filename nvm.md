Instalar una versión 
```bash
nvm install 22.19.0
```

Crea un alias para la versión 
```bash
nvm alias default 22.19.0
```

Usar la versión por su alias
```bash
nvm use default
```
Eliminamos la versión 
```bash
nvm uninstall v22.18.0
```

Eliminamos un alias 
```bash
nvm unalias default
```
